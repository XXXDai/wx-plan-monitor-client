requests>=2.31
PyYAML>=6.0

# 微信 4.1+（含 4.1.8.107）：免费版 wxauto4，仅 Windows + Python 3.9-3.12
# 本项目只用免费版，不使用 Plus 版 wxautox4
wxauto4>=41.1.0; sys_platform == "win32"

# 微信 3.9.x 的老客户端（同时把 config.yaml 的 monitor.backend 设为 wxauto）：
# wxauto>=3.9.11; sys_platform == "win32"

# macOS / Linux 上装不了以上任何一个，用 --mock-dir 模拟模式调试
